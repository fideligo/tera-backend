# Secrets — only .env.example is committed (BUILD_SPEC 4.5)
.env
*.env
.env.local
.env.*.local
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node (Phase 2)
node_modules/
.next/
out/

# Flutter (Phase 3)
.dart_tool/
.flutter-plugins*
**/build/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# The proposal is the product source of truth, but a 3.8 MB binary re-committed on every
# revision bloats history permanently. The authoritative copy is the one submitted to Google
# Classroom; BUILD_SPEC.md and docs/decisions.md are what this repo needs to carry.
docs/proposal.pdf
